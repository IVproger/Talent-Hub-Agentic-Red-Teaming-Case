# Python
.venv/
__pycache__/
*.pyc

# Secrets / local API keys — never commit
.env
*.key
keys.local

# macOS
.DS_Store

# PoC run outputs — everything produced by a vulnerability run
# (report.md, findings.json, knowledge.jsonl and any future artifacts); regenerated per run
poc/out/
